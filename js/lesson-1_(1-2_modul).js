'use strict';


// Завдання 1:
// Запропонуйте користувачу ввести число за допомогою prompt().
// Збережіть введене значення у змінну number.
// Перевірте, чи дорівнює змінна number числу 10:
//    Якщо так – виведіть повідомлення 'Вірно' у alert().
//    Якщо ні – виведіть 'Невірно'.

// Підказка: prompt завжди повертає рядок (String), тому перед перевіркою
// перетворіть значення на число за допомогою Number().

// let number = prompt("Введіть число:");

// if (Number(number) === 10) {
//     alert("Вірно");
//   } else {
//     alert("Невірно");
//   }


// ......................................
// ......................................


// Завдання 2:

// У змінній min лежить випадкове число від 0 до 59.
// Визначте, в яку чверть години потрапляє
// це число (у першу, другу, третю чи четверту).
// Виведіть в alert відповідне повідомлення, наприклад:
// "10 входить в першу чверть"

// Шаблонна формула для будь-якого діапазону. 
// Math.random(): повертає випадкове число в діапазоні від 0 (включно) до 0.999999 (але ніколи 1.0):
// Math.floor(Math.random() * (max - min) + min);
// const min = Math.floor(Math.random() * (59 - 0) + 0);

// Для нашого випадку де min=0, можна писати просто:
// const min = Math.floor(Math.random() * 59;

// ВАРІАНТ-1
// const min = Math.floor(Math.random() * 59);

// if(min <= 15) {
// alert(`${min} входить до першої чверті`);
// } else if(min <= 30) {
// alert(`${min} входить до другої чверті`);
// } else if(min <= 45) {
//    alert(`${min} входить до третьої чверті`); 
// } else {
//     alert(`${min} входить до четвертої чверті`);
// }


// .................


// ВАРІАНТ-2   практика Blended

// const min = Math.floor(Math.random() * 59);

// if(min >= 0 && min < 15) {
// alert(`${min} входить до першої чверті`);
// } else if(min >= 15 && min < 30) {
// alert(`${min} входить до другої чверті`);
// } else if(min >= 30 && min < 59) {
//    alert(`${min} входить до третьої чверті`); 
// } else {
//     alert(`${min} входить до четвертої чверті`);
// }


// .................


// ВАРІАНТ-3   практика Blended
// const min = Math.floor(Math.random() * 59);

// let textMin;

// if(min >= 0 && min < 15) {
//  textMin = "першої";
// } else if(min >= 15 && min < 30) {
//  textMin = "другої";
// } else if(min >= 30 && min < 45) {
//    textMin = "третьої";
// } else {
//      textMin = "четвертої";
// }

// alert(`${min} входить до ${textMin} чверті`);


// .................


// ВАРІАНТ-4   Чат GPT - оператор switch. 
// switch не може працювати з умовами (case з >, <) — switch порівнює значення точно (===), а не по діапазонах. "case (min > 0 && min <= 15)" - БУДЕ ПОМИЛКА !!! Тому що min (наприклад, число 10) порівнюється з результатом виразу (min > 0 && min <= 15) (який повертає true або false). Тобто порівнюється 10 === true, що завжди false.
// Працюючий варіант (ХИТРІСТЬ):

// const min = Math.floor(Math.random() * 59);  

// // const min = Number(prompt("Введіть число від 0 до 59:"));

// let textMin;

// switch (true) {
//     case (min >= 0 && min <= 14):
//         textMin = "першої";
//         break;
//     case (min >= 15 && min <= 29):
//         textMin = "другої";
//         break;
//     case (min >= 30 && min <= 44):
//         textMin = "третьої";
//         break;
//     case (min >= 45 && min <= 59):
//         textMin = "четвертої";
//         break;
//     default:
//         textMin = "невідомої";
// }

// alert(`${min} входить до ${textMin} чверті`);


// ......................................
// ......................................


//     Завдання 3:

// // Змінна num може набувати 4 значення: '1', '2', '3' або '4'
// // (запитуй це значення у користувача через prompt).
// // Якщо вона має значення '1', то у змінну result запишіть 'зима',
// // якщо має значення '2' - 'весна' і так далі.
// // Розв'яжіть завдання через switch-case.
// // Не забудьте про дефолтне значення, на випадок, якщо користувач
// // введе в prompt щось інше. В такому випадку result має набувати значення:
// // "Вибачте, але ви маєте ввести значення від 1 до 4 включно".
// // Значення змінної result виведіть в консоль.

// const num = Number(prompt("Введіть число:"));

// let result;

// switch (num) {    
// case 1:
//         result = "зима";
// break;
// case 2:
//       result = "весна";
// break;
// case 3:
//       result = "літо";
// break;
// case 4:
//       result = "осінь";
// break;
// default:
//    result = `Вибачте, але ви маєте ввести значення від 1 до 4 включно`;
// }

// console.log(result);


//..................................................
//..................................................


// Завдання 4:

// // Отримуйте від користувача число (кількість хвилин) через prompt
// // і виводьте у консоль рядок у форматі годин та хвилин.
// // Приклад: користувач вводить в prompt '70' -> в консолі відобразиться '01:10'.
// // Корисне посилання для відображення годин та хвилин у потрібному форматі ('01' замість '1'):
// // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/padStart#examples



// ВАРІАНТ-1

// // 1. Отримуємо кількість хвилин від користувача
// const data = prompt("Введіть кількість хвилин:");

// // 2. Перетворюємо введене значення в число
// const number = Number(data);
// console.log(number);

// // 3. Обчислюємо години (ціла частина від ділення на 60)
// const hours = Math.floor(number / 60);
// console.log(hours);

// // 4. Обчислюємо хвилини (залишок від ділення на 60)
// const minutes = number - hours * 60;
// console.log(minutes);

// const minutes2 = (number % 60);
// console.log(minutes2);

// // 5. Форматуємо години та хвилини у двоцифровий формат
// // padStart(2, '0') додає '0' на початок, якщо число менше 2 цифр
// const formattedHours = String(hours).padStart(2, '0');
// console.log(formattedHours);

// const formattedMinutes = String(minutes).padStart(2, '0');
// console.log(formattedMinutes);


// const result = `${formattedHours}:${formattedMinutes}`
// console.log(result);
// alert(result);


// .....................................
//  ....................................


// // ВАРІАНТ-2

//  // 1. Отримуємо кількість хвилин від користувача
// let number = prompt("Введіть кількість хвилин:");

// // 2. Перетворюємо введене значення в число
// const num = Number(number);

// // 3. Обчислюємо години (ціла частина від ділення на 60)
// const hours = Math.floor(num / 60);
// console.log(hours);

// // 4. Обчислюємо хвилини (залишок від ділення на 60 - Скільки разів 60 поміщається в num)
// const minutes = Math.round(num % 60);
// console.log(minutes);

// // 5. Форматуємо години та хвилини у двоцифровий формат - перетворюємо спочатку до рядка.
// // padStart(2, '0') додає '0' на початок, якщо число менше 2 цифр
// const formattedHours = String(hours).padStart(2, '0');
// console.log(formattedHours);

// const formattedMinutes = String(minutes).padStart(2, '0')
// console.log(formattedMinutes);

// // 6. Відображення годин та хвилин в консолі у форматі '01:10' :
// const result = `${formattedHours}:${formattedMinutes}`
// console.log(result);

// // можна вивести через alert:
// alert(result);


// ................................
//  ...............................



// Завдання 5:

// // Напишіть код, який запитуватиме у користувача
// // логін за допомогою prompt і виводить результат в консоль браузера

// // Після цього додайте перевірку введеного користувачем значення:
// // Якщо відвідувач вводить "Адмін",
// // то prompt запитує пароль (не забудьте зберігти його у змінну для подальшої перевірки).

// // Якщо нічого не ввели ("") або натиснули Cancel (prompt() повертає null тільки при Cancel або Esc)
// // Вивести в alert строку "Скасовано"

// // В іншому випадку вивести в alert рядок "Я вас не знаю"

// // Пароль перевіряти так:
// // Якщо введено пароль "Я головний",
// // то вивести в alert рядок "Добрий день!",
// // в іншому випадку вивести в alert рядок "Невірний пароль!"



// ВАРІАНТ-1

// const user = prompt ("Введіть логін:");
// console.log(user);

// // prompt()  повертає null тільки при Cancel або Esc
// if (user === null) {
//   alert("Скасовано");
// } else {
//   // Потім обробляємо текст і ПРОБІЛИ
//   const userTrimmed = user.trim();
// if (userTrimmed === "") {
//   alert("Ви нічого не ввели!");
// } else if (userTrimmed === "Адмін") {
// const password = prompt("Введіть пароль:");
// alert (password === "111111" ? "Добрий день!" : "Невірний пароль!");

// // Можливі покращення: Cancel при вводі пароля
// if (password === null) {
//   alert("Скасовано");
// } else if (password === "111111") {
//   alert("Добрий день!");
// } else {
//   alert("Невірний пароль!");
// }

// }
// else {
//   alert("Я вас не знаю");
// }
// }


//  .......................


// ВАРІАНТ-2

// const user = prompt("Введіть логін:");

// let password;

// if (user === "Адмін") {
//   password = prompt("Введіть пароль:");  
// } else if(user === "" || user === null) {
// alert("Скасовано");

// } else {
//   alert("Я вас не знаю");
// }

// // Робота з паролем:

// if (password === "" || password === null) {
// alert("Скасовано");
//   } else if(password === "Я головний") {
// alert("Добрий день!")
// } else {
// alert("Невірний пароль!");
// }


//  ....................................


// ВАРІАНТ-3  Використання ФУНКЦІЇ

// const user = prompt("Введіть логін:");

// console.log(fooAccess(user));

// function fooAccess(login) {
// let password;

// if (login === "Адмін") {
//   password = prompt("Введіть пароль:");  
   
// } else if(login === "" || login === null) {
// alert("Скасовано");
// return;

// } else {
//   alert("Я вас не знаю");
//   return;
// }

// // Робота з паролем:

// if (password === "" || password === null) {
// alert("Скасовано");
//   } else if(password === "Я головний") {
// alert("Добрий день!")
// } else {
// alert("Невірний пароль!");
// }
// }


//  ....................................
//  ....................................



// Завдання 6:

// Використайте цикл while, щоб вивести в console всі числа від 0 до 20 включно.

// ВАРІАНТ-1
// let i = 0;
// while (i <= 20) {
//   console.log(i);
//   i += 1;
// }

// ВАРІАНТ-2
// let number = 0;
// while (number <= 20) {
//   console.log(`${number}`);
//   number += 1;
// }


//  ....................................
//  ....................................



// Завдання 7:

// // Напишіть функцію getNumbers(min, max), що приймає 2 параметри -
// // мінімальне і максимальне число відповідно.
// // Напишіть цикл всередині функції, який виводить у консоль
// // всі числа від max до min за спаданням.
// // Окрім цього, підрахуйте суму всіх парних чисел в цьому циклі
// // і поверніть її з функції.


// console.log(getNumbers(2, 11));

// function getNumbers(min, max) {
//   let sum = 0;
//   for(let i = max; i >= min; i -= 1 ) {
// console.log(i);
// if(!(i % 2)) {
// sum += i;
// }
//   }
//   return sum;
// }


//  ....................................
//  ....................................



// Завдання 8:

// // Напишіть функцію min(a, b), яка приймає 2 числа і повертає меньше з них.
// // Потрібно додати перевірку, що функція отримує саме числа, в іншому випадку
// // повертати з функції рядок - 'Not a number!'.


// console.log(min(5, 22));

// function min(a, b) {
// if(!isNaN(a) && !isNaN(b)) {
// return Math.min(a, b);
// }
// return 'Not a number!';
// }



//  ....................................
//  ....................................



// Завдання 9:

// // Напишіть функцію isAdult(age), яка  приймає число - вік користувача і повертає true, 
// // якщо параметр age більше чи дорівнює 18.
// // В іншому випадку вона запитує підтвердження через confirm 
// // і повертає його результат (true/false).

// const result = confirm("Ви впевнені?");
// "Запитує підтвердження через confirm" означає, що код використовує 
// вбудовану функцію JavaScript confirm(), 
// яка показує модальне вікно з питанням і двома кнопками.
// Показує діалогове вікно з вашим питанням.
// Користувач обирає:

// OK (Так/Підтвердити) → повертає true
// Скасувати (Ні/Відмінити) → повертає false

// Блокує виконання коду доки користувач не відповість


// console.log(isAdult(5));

// function isAdult(age) {
// return age >= 18 ? true : confirm("Ви впевнені?")
// }


//  ....................................
//  ....................................



// Завдання 10:

// Напишіть функцію fizzBuzz(num), яка приймає число і перевіряє кожне число від 1 до num:
// Якщо число ділитися  без остачі на 3 - виводить в консоль 'fizz',
// якщо ділиться  без остачі на 5 - виводить в консоль 'buzz',
// якщо ділиться  без остачі і на 3, і на 5 - виводить в консоль 'fizzbuzz'.


// fizzBuzz(15);

// function fizzBuzz(num) {

// for(let i = 1; i <= num; i+=1) {

//     // console.log(i);
    
// if(i % 3 === 0 && i % 5 === 0) {
//     console.log("fizzbuzz");    
// } else if(i % 3 === 0) {
// console.log("fizz"); 
// } else if(i % 5 === 0) {
// console.log("buzz"); 
// } 
// }
// }
















